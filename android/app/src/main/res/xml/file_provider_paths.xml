<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- External files dir — camera captures go here -->
    <external-files-path name="camera_images" path="Pictures/"/>
    <!-- Internal cache — fallback for older devices -->
    <cache-path name="cache_images" path="images/"/>
    <!-- External cache -->
    <external-cache-path name="external_cache_images" path="."/>
</paths>
